# Backend Environment Variables
PORT=3001

# OpenAI API Key (server-side only)
OPENAI_API_KEY=

# SMTP (for Email Schedule)
# Example (Google Workspace / Microsoft / SMTP relay):
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="RestoreLogic Dispatch <dispatch@yourdomain.com>"

