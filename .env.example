# Supabase Configuration
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# OpenAI API Key (server-side only — used by backend and Vercel API routes)
# Set this in backend/.env for local dev and in Vercel Environment Variables for production.
# Do NOT prefix with VITE_ — it must never be exposed to the browser.
# OPENAI_API_KEY=sk-your-openai-api-key
